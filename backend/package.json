{
  "name": "json-server-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch jobs.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
